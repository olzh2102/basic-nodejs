const aCode = 'a'.charCodeAt()
const ACode = 'A'.charCodeAt()

const ALLOWED_FLAGS = ['-c', '--config', '-i', '--input', '-o', '--output']

module.exports = {
    aCode,
    ACode,
    ALLOWED_FLAGS
}