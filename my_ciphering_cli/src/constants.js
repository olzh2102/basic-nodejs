const A = 'a'
const CAPITAL_A = 'A'

const Z = 'z'
const CAPITAL_Z = 'Z'

const ATBASH_FLAG = 'atb'

const ALPHABET_SIZE = 26

const CAESAR_SHIFT = {
    ENCODE: 1,
    DECODE: -1
}

const ROT8_SHIFT = {
    ENCODE: -8,
    DECODE: 8
}

const ALPHABER_LAST_POSITION = 25

const MARKS = {
    C1: 'C1',
    C0: 'C0',
    R1: 'R1',
    R0: 'R0',
    A: 'A'
}

module.exports = {
    A,
    CAPITAL_A,
    Z,
    CAPITAL_Z,
    ATBASH_FLAG,
    ALPHABET_SIZE,
    CAESAR_SHIFT,
    ROT8_SHIFT,
    ALPHABER_LAST_POSITION,
    MARKS
}